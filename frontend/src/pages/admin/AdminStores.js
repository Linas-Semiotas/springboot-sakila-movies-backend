
const AdminStores = () => {

    return (
        <div className="user-container">
            <h2>Stores</h2>
            placeholder
            {/*error && <p className="error-message">{error}</p>*/}
        </div>
    );
};

export default AdminStores;
